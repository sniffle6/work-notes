## Highlights
- Fixed the Work Notes taskbar icon so it uses the app icon instead of falling back to the default Cargo/Tauri artwork.

## Changes
- Set the Windows taskbar icon explicitly in addition to the tray and window icon.
- Kept the existing tray icon behavior unchanged.

## Install And Update Notes
- Windows installer is attached to the GitHub release.
- Existing installs update from the latest-release `latest.json` after publish.
